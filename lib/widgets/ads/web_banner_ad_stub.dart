import 'package:flutter/material.dart';

Widget buildWebBannerAd(double width, double height) {
  return SizedBox(
    width: width,
    height: height,
    // Return empty space or a placeholder on mobile
  );
}
